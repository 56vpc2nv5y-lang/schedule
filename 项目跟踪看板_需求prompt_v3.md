# 项目跟踪看板 — 开发需求 Prompt（v3，Vercel 部署版）

> 使用方式：将本文件全文粘贴给 Claude Code，作为第一条指令，让它据此在 Next.js + Vercel 技术栈下搭建应用。

---

## 一、背景与角色

我是技术合作项目的中间商，日常在「甲方（境外客户：新加坡/马来西亚/澳门/香港等）」和「供应商（各高校技术团队）」之间协调推进。目前作为新人，实际经历过的流程走到"会议纪要循环校对"这一步，但业务逻辑上后面必然还有**合同签署、项目交付/实施、验收、售后维护**等阶段——请按一个技术方案中间商业务的完整生命周期来设计阶段模板，不要只覆盖我目前经历过的部分。

**部署方式**：Next.js，部署在 Vercel。

---

## 二、项目默认阶段模板（Default Stage Template）

这是一整套项目从头到尾可能经历的阶段，作为系统内置的默认模板，允许在「设置」里增删改（因为不同项目类型走的阶段可能不一样，比如有的项目没有正式招投标环节）：

1. 商机/需求对接（客户提出需求或我方主动匹配供应商）
2. 供应商匹配与方案沟通
3. 报价/预算沟通
4. 技术方案/PPT 制作与审查
5. 甲方会议 + 供应商会议
6. 会议纪要循环校对（多轮，见下文子模块）
7. 合同谈判
8. 合同签署
9. 项目实施/交付跟进
10. 验收
11. 售后维护/年度回访
12. 归档/复盘

每个阶段依然保留：计划开始/结束日期、实际完成日期、状态（未开始/进行中/已完成/已延期）、关联任务、关联文件、关联对接人——这套结构不变，只是阶段清单更完整了，能撑起甘特图从项目起点画到终点。

**重要**：这是"模板"而不是写死的固定流程。新建项目时可以从模板一键生成这些阶段，也可以在设置里编辑模板本身（增加/删除/重命名阶段、调整默认顺序），因为随着我经历更多项目，流程认知会更新，系统要跟得上。

---

## 三、核心数据模型（延续 v2，做了关联同步的强化）

### 1. 项目（Project）
名称（中/英）、甲方名称、地区标签、状态、起止日期、备注

### 2. 团队与对接人（挂在项目下，引用联系人库，不重复录入）
我方团队、供应商团队+主要对接人、甲方团队+主要对接人

### 3. 项目阶段（按第二节模板生成，可编辑）

### 4. 任务/事项
类型：商务沟通 / 甲方会议 / 供应商会议 / 甲方邮件问题 / 供应商文件问题 / PPT制作翻译审查 / 会议纪要循环校对 / 合同跟进 / 交付跟进 / 验收跟进 / 售后跟进 / 接待安排 / 展会邀请函

### 5. 项目文件库
文件名、类型（会议纪要/问题反馈清单/PPT/合同/验收单/邀请函/Brochure等）、版本号、关联阶段、状态、更新时间

### 6. 会议纪要循环校对（多轮往返，逻辑不变）

### 7. 项目动态时间线（自动生成，状态变化自动记录）

### 8. 接待安排（外方来访 / 展会邀请，两个子类型不变）

### 9. 联系人库
姓名、单位、职位、地区、邮箱、微信、角色（甲方对接人/供应商对接人/我方团队/来访嘉宾等，可多选）、关联项目

---

## 四、关联数据的同步机制（这次重点新增）

现在的设计里很多字段是"独立文本"，容易出现改了一处、别处没跟着改的问题。要求：

1. **所有关联字段用引用（ID关联），不用复制文本**。比如项目里的"供应商主要对接人"，存的是联系人库里那条记录的引用，不是手动打的名字——这样我在联系人库里改了这个人的职位或邮箱，项目详情页、任务里显示的也自动是最新的，不用逐处改。

2. **级联选择**：下拉选择要互相联动，减少重复选择和出错：
   - 新建任务时先选"关联项目"，供应商/甲方对接人的下拉自动过滤成这个项目已经关联的那几个人，不用从全量联系人库里翻
   - 选"甲方地区"后，"甲方对接人"下拉自动过滤成该地区已有的联系人
   - 新建接待安排时，来访人可以直接从联系人库选，选中后自动带出其单位、职位，不用重新打字

3. **状态联动提醒（不强制自动改，但要提示）**：比如一个阶段下所有任务都标记"已完成"时，系统提示"是否将该阶段状态一并标记为已完成？"；会议纪要循环某一轮定稿后，提示"是否将对应文件存入文件库"——这类联动做成"提示确认"而不是"静默自动改"，避免我没注意到系统自己动了什么。

4. **联系人库作为唯一数据源（Single Source of Truth）**，项目、任务、接待安排、文件库里凡是"人"的字段，全部引用联系人库，不允许自由文本输入一个新名字而不落库。

---

## 五、设置栏（Settings）— 新增模块

需要一个独立的设置页面，至少包含：

- **阶段模板管理**：编辑第二节的默认阶段模板（增删改阶段名称、默认顺序），支持针对不同项目类型建多套模板（比如"标准项目模板" vs "接待/展会专项模板"）
- **标签管理**：甲方地区标签、项目类型标签、任务类型标签，可增删
- **联系人角色管理**：定义联系人可选的角色类型
- **团队成员管理**：维护"我方团队"名单，供任务分配、项目团队选择时下拉使用
- **文件类型/模板管理**：常用文件类型清单，以及邀请函这类需要变量替换的模板（先做简单的文本模板+变量占位符）
- **显示偏好**：中英文显示方式（默认中文/中英并排等）
- **数据备份**：导出全部数据（JSON）、导入恢复——因为部署在 Vercel 用了数据库，仍建议做这个功能防止误删或迁移

---

## 六、看板视图设计（沿用 v2，视图列表不变）

1. 甘特图视图（默认首页，现在能画完整生命周期）
2. 项目详情页（阶段时间轴+团队对接人+文件库+动态时间线）
3. 项目看板视图（按状态分列卡片）
4. 会议纪要循环追踪视图
5. 任务列表视图
6. 日历视图
7. 接待安排详情页
8. 联系人库列表
9. **设置页**（新增）

---

## 七、技术栈建议（Vercel 部署，请按此实现，除非有更合适的替代方案再告诉我）

- 框架：Next.js（App Router）+ TypeScript
- 数据库：**不要只用浏览器 localStorage**——部署到 Vercel 后我可能换设备/清缓存，数据会丢，且局域网内数据不共享。建议用 Vercel Postgres 或 Supabase（免费额度够用），配合 Prisma 作为 ORM
- UI：Tailwind CSS + shadcn/ui 组件库，界面简洁、信息密度高
- 甘特图：使用现成的 React 甘特图组件库（比如 frappe-gantt 或类似的开源库），不用从零画
- 登录/权限：单人使用，先做一个简单的密码保护页面即可，不需要复杂的用户体系
- 表单联动：下拉选择的级联逻辑（见第四节）用前端状态管理实现即可，不需要额外中间件

---

## 八、取舍说明

- 阶段模板做成"可配置"而不是写死，是因为我现在只走到会议纪要这一步，后面合同、交付、验收具体怎么走，等真正经历过才会更准确，系统需要跟得上我认知的更新，不能靠这次一次性画对。
- 关联数据全部走引用而不是复制文本，短期开发成本高一点，但这是避免以后"改了这里没改那里"的根本方法，值得优先做对。
- 邮件模板库、按人头看工作量的视图这类，还是先不加，等基础版（甘特图+文件库+动态日志+设置里的模板管理）跑顺了再迭代。

---

请先给出：① 数据库表结构设计（体现关联引用而非文本复制）、② 页面路由结构、③ 阶段模板的默认数据。我确认后再开始写代码。
